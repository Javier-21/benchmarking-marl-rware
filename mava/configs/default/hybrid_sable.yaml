# This file is create by Javier-21 in May 2026.
# It contains the configuration of a variant of rec_sable that loads the params from a ff_sable checkpoint.
defaults:
  - logger: logger
  - arch: anakin
  - system: sable/hybrid_sable
  - network: rec_retention
  - env: rware # [cleaner, connector, vector-connector, gigastep, lbf, matrax, rware, smax, mpe]
  - _self_

hydra:
  searchpath:
    - file://mava/configs
